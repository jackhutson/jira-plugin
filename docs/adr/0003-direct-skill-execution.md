# Execute through direct outcome skills

Jira requests route directly to outcome-specific skills; the plugin has no
session-wide mode question, generic router, or custom Jira agent. Forked context
is reserved for a future self-contained workflow that can receive all required
input explicitly, because implicit conversation context—especially inline plans
for decomposition—would otherwise be lost.
